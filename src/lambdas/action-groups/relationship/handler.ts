/** Relationship collaborator-agent action group. Handles all Relationship use cases. */
import { makeActionGroupHandler } from "../../../shared/action-group.js";

export const handler = makeActionGroupHandler("Relationship");
