/** XShipReport collaborator-agent action group. Handles all XShipReport use cases. */
import { makeActionGroupHandler } from "../../../shared/action-group.js";

export const handler = makeActionGroupHandler("XShipReport");
