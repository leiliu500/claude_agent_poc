/** EDD collaborator-agent action group. Handles all EDD use cases. */
import { makeActionGroupHandler } from "../../../shared/action-group.js";

export const handler = makeActionGroupHandler("EDD");
