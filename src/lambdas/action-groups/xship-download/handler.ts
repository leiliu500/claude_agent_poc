/** XShipDownload collaborator-agent action group. Handles all XShipDownload use cases. */
import { makeActionGroupHandler } from "../../../shared/action-group.js";

export const handler = makeActionGroupHandler("XShipDownload");
