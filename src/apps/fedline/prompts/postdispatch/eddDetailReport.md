# eddDetailReport — operation-specific overlay

Overrides the EDD family overlay for the `eddDetailReport` operation only. Only the sections present
here override the family; unspecified roles fall back to EDD.md.

## Analytics

OPERATION — EDD DETAIL: line-level records for a single prepared report. Prioritise line-level outliers and the composition of the difference total for this one report — not cross-institution comparison.
